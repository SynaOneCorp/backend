ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080
    environment:
        oauthClient: $OAUTH_CLIENT
        oauthSecret: $OAUTH_SECRET